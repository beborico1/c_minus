int array[10];

void main(void) {
    int i;
    
    /* Inicializar arreglo */
    i = 0;
    while (i < 10) {
        array[i] = i * i;
        i = i + 1;
    }
    
    /* Acceder al arreglo */
    i = array[5] + array[2];
}