void main(void) {
    int x;
    x = 10;
    
    /* Prueba if simple */
    if (x > 5) 
        x = x - 1;
    
    /* Prueba if-else */
    if (x == 9) 
        x = x * 2;
    else 
        x = x / 2;
    
    /* Prueba while */
    while (x > 0) 
        x = x - 1;
}