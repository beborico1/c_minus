void main(void) {
    int a; int b; int c; int result;
    
    a = 5;
    b = 10;
    c = 2;
    
    /* Operadores aritmeticos y precedencia */
    result = a + b * c;
    result = (a + b) * c;
    result = a - b / c + a * (b - c);
    
    /* Operadores relacionales */
    if (a < b && b <= c || a != c) 
        result = 1;
}