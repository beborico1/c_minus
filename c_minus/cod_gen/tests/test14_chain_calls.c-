/* Test 14: Chain of function calls */

int addOne(int x) {
    return x + 1;
}

int addTwo(int x) {
    return x + 2;
}

int addThree(int x) {
    return x + 3;
}

void main(void) {
    output(addThree(addTwo(addOne(10)))); /* Should output 16 */
}