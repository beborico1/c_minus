int factorial(int n) {
    if (n <= 1) {
        return 1;
    } else {
        return n * factorial(n - 1);
    }
}

void main(void) {
    int x;
    int result;
    
    x = 5;
    result = factorial(x);
    output(result);
}